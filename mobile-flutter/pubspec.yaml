name: stockpilot_mobile
description: StockPilot Operational Mobile Application for inventory scanning, POS sales, and demand tracking.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  intl: ^0.19.0
  provider: ^6.1.1
  lucide_icons: ^0.257.0
  flutter_secure_storage: ^9.2.2
  flutter_local_notifications: ^17.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
